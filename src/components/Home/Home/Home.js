import React from 'react';
import Header from '../Header/Header';

const Home = () => {
    return (
        <div style={{height: '70vh'}} className="container">
            <Header/>
        </div>
    );
};

export default Home;